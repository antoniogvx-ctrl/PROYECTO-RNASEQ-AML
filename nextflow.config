docker {
    enabled = true
    autoMounts = true
    runOptions = '-u $(id -u):$(id -g)'
}

process {
    // Configuración por defecto para los procesos
    shell = ['/bin/bash', '-euo', 'pipefail']
}
